services:
  - type: web
    name: cortex-ai-workspace
    env: python
    plan: free
    buildCommand: pip install --upgrade pip setuptools wheel && pip install --no-cache-dir -r requirements-minimal.txt
    startCommand: python -m uvicorn app_minimal:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
      - key: ENVIRONMENT
        value: production
    autoDeploy: false

databases:
  - name: cortex-ai-db
    plan: free
    databaseName: cortex_ai_workspace
    user: cortex_user